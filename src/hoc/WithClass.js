import React from 'react';
import { ProgressPlugin } from 'webpack';

const withClass = (WrappedComponent, className) => (
<div className = {prompt.classes} > {props.children}</div>
);
 export default withClass;